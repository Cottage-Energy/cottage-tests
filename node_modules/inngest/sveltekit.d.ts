import { type RequestEvent } from "@sveltejs/kit";
import { type ServeHandlerOptions } from "./components/InngestCommHandler";
import { type SupportedFrameworkName } from "./types";
export declare const frameworkName: SupportedFrameworkName;
/**
 * Using SvelteKit, serve and register any declared functions with Inngest,
 * making them available to be triggered by events.
 *
 * @example
 * ```ts
 * export const { GET, POST, PUT } = serve(...);
 * ```
 *
 * @public
 */
export declare const serve: (options: ServeHandlerOptions) => ((event: RequestEvent<Partial<Record<string, string>>, string | null>) => Promise<Response>) & {
    GET: (event: RequestEvent<Partial<Record<string, string>>, string | null>) => Promise<Response>;
    POST: (event: RequestEvent<Partial<Record<string, string>>, string | null>) => Promise<Response>;
    PUT: (event: RequestEvent<Partial<Record<string, string>>, string | null>) => Promise<Response>;
};
//# sourceMappingURL=sveltekit.d.ts.map