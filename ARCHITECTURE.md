# 🏗️ AI Framework Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    Playwright Test Framework                     │
│                  Enhanced with AI Capabilities                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────┐
        │      Base Test Fixture                   │
        │   (pg_page_base.ts)                     │
        │                                          │
        │  • Standard Page Objects                 │
        │  • AI Test Utilities                     │
        │  • MCP Helper                            │
        │  • Database Queries                      │
        └─────────────────────────────────────────┘
                              │
                ┌─────────────┴─────────────┐
                ▼                           ▼
    ┌───────────────────┐       ┌───────────────────┐
    │  AI Test Utilities │       │   MCP Helper      │
    │  (aiTestUtilities) │       │  (mcpHelper)      │
    └───────────────────┘       └───────────────────┘
                │                           │
                ▼                           ▼
    ┌───────────────────┐       ┌───────────────────┐
    │  Claude 3.5       │       │  MCP Server       │
    │  Sonnet API       │       │  (Playwright)     │
    └───────────────────┘       └───────────────────┘
```

---

## Component Architecture

### 1. Test Execution Flow

```
Test Start
    │
    ├─► beforeEach
    │       │
    │       └─► Setup (DB, config, navigation)
    │
    ├─► Test Execution
    │       │
    │       ├─► AI Flow Validation (optional)
    │       ├─► MCP Automation (optional)
    │       └─► Test Logic
    │
    ├─► Test Completion
    │       │
    │       └─► Assertions
    │
    └─► afterEach
            │
            ├─► Check if failed?
            │       │
            │       └─► Yes → AI Failure Analysis
            │               │
            │               ├─► Capture screenshot
            │               ├─► Get error details
            │               ├─► Send to Claude AI
            │               ├─► Receive analysis
            │               └─► Save insights
            │
            └─► Cleanup (DB, browser)
```

---

## 2. AI Test Utilities Architecture

```
┌─────────────────────────────────────────────────────────┐
│              AITestUtilities Class                       │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────────────────────────────────┐        │
│  │         Anthropic Client                    │        │
│  │  (Claude 3.5 Sonnet API)                   │        │
│  └────────────────────────────────────────────┘        │
│                      │                                   │
│          ┌───────────┴───────────┐                     │
│          ▼                       ▼                      │
│  ┌──────────────┐      ┌──────────────────┐           │
│  │   Vision     │      │   Text Analysis   │           │
│  │   Analysis   │      │                   │           │
│  └──────────────┘      └──────────────────┘           │
│          │                       │                      │
│          └───────────┬───────────┘                     │
│                      ▼                                  │
│           ┌──────────────────────┐                     │
│           │  AI Capabilities      │                     │
│           ├──────────────────────┤                     │
│           │ • Failure Analysis    │                     │
│           │ • Flow Validation     │                     │
│           │ • Data Generation     │                     │
│           │ • Flakiness Detection │                     │
│           │ • Refactoring Tips    │                     │
│           │ • Selector Suggestions│                     │
│           └──────────────────────┘                     │
└─────────────────────────────────────────────────────────┘
```

---

## 3. MCP Helper Architecture

```
┌─────────────────────────────────────────────────────────┐
│           MCPPlaywrightHelper Class                      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────────────────────────────────┐        │
│  │         MCP Client                          │        │
│  │  (Model Context Protocol)                  │        │
│  └────────────────────────────────────────────┘        │
│                      │                                   │
│                      ▼                                   │
│  ┌────────────────────────────────────────────┐        │
│  │     StdioClientTransport                    │        │
│  │  (Communication with MCP Server)           │        │
│  └────────────────────────────────────────────┘        │
│                      │                                   │
│          ┌───────────┴───────────┐                     │
│          ▼                       ▼                      │
│  ┌──────────────┐      ┌──────────────────┐           │
│  │  Browser     │      │  Performance      │           │
│  │  Automation  │      │  Monitoring       │           │
│  └──────────────┘      └──────────────────┘           │
│          │                       │                      │
│          └───────────┬───────────┘                     │
│                      ▼                                  │
│           ┌──────────────────────┐                     │
│           │  MCP Capabilities     │                     │
│           ├──────────────────────┤                     │
│           │ • Navigate            │                     │
│           │ • Screenshot          │                     │
│           │ • Performance Metrics │                     │
│           │ • Network Monitoring  │                     │
│           │ • Accessibility Tree  │                     │
│           │ • Natural Language    │                     │
│           └──────────────────────┘                     │
└─────────────────────────────────────────────────────────┘
```

---

## 4. AI Reporter Flow

```
Test Run Start
      │
      ▼
┌─────────────┐
│   onBegin   │  → Initialize stats, config
└─────────────┘
      │
      ▼
┌─────────────┐
│ onTestBegin │  → Log test start
└─────────────┘
      │
      ▼
┌─────────────┐
│  onTestEnd  │  → Collect result data
└─────────────┘  → Store in array
      │
      │  (repeat for all tests)
      │
      ▼
┌─────────────┐
│    onEnd    │  → Generate statistics
└─────────────┘  → Call AI for summary
      │            → Create reports
      ▼
┌─────────────────────────────┐
│  AI Analysis                 │
│  • Pattern detection         │
│  • Failure clustering        │
│  • Recommendations           │
│  • Trend analysis            │
└─────────────────────────────┘
      │
      ▼
┌─────────────────────────────┐
│  Output Files                │
│  • ai-test-summary.md        │
│  • ai-test-report.json       │
│  • ai-insights/*.json        │
└─────────────────────────────┘
```

---

## 5. Data Flow Diagram

```
┌──────────────┐
│  Test File   │
└──────────────┘
       │
       ▼
┌──────────────┐      ┌─────────────────┐
│ Test Fixture │─────→│  Page Objects   │
└──────────────┘      └─────────────────┘
       │                      │
       ├──────────────────────┤
       │                      │
       ▼                      ▼
┌──────────────┐      ┌─────────────────┐
│ AI Utilities │      │   MCP Helper    │
└──────────────┘      └─────────────────┘
       │                      │
       ▼                      ▼
┌──────────────┐      ┌─────────────────┐
│  Claude API  │      │   MCP Server    │
└──────────────┘      └─────────────────┘
       │                      │
       └──────────┬───────────┘
                  ▼
         ┌────────────────┐
         │  Test Results  │
         └────────────────┘
                  │
                  ▼
         ┌────────────────┐
         │  AI Reporter   │
         └────────────────┘
                  │
                  ▼
         ┌────────────────┐
         │  Report Files  │
         └────────────────┘
```

---

## 6. File Structure

```
cottage-tests/
│
├── tests/
│   ├── e2e_tests/
│   │   ├── ai_enhanced_examples.spec.ts   ← Example tests
│   │   └── move_in/
│   │       └── workflows/
│   │           └── move_in_shortcode.spec.ts  ← Enhanced with AI
│   │
│   └── resources/
│       ├── fixtures/
│       │   ├── aiTestUtilities.ts         ← AI capabilities
│       │   ├── mcpPlaywrightHelper.ts     ← MCP integration
│       │   ├── aiTestReporter.ts          ← AI reporter
│       │   └── ... (other fixtures)
│       │
│       └── page_objects/
│           └── base/
│               └── pg_page_base.ts        ← Base fixture
│
├── test-results/
│   ├── ai-test-summary.md                 ← AI summary (generated)
│   ├── ai-test-report.json                ← AI report (generated)
│   └── ai-insights/                       ← Individual analyses
│
├── .env                                    ← API keys
├── playwright.config.ts                    ← Config with AI reporter
├── package.json                            ← Dependencies
│
├── AI_FRAMEWORK_README.md                  ← Complete guide
├── QUICK_START.md                          ← Getting started
└── IMPLEMENTATION_SUMMARY.md               ← This summary
```

---

## 7. API Integration Points

### Anthropic Claude API

```
Test → AITestUtilities → Anthropic SDK → Claude API
                              │
                              ├─► analyzeFailure()
                              ├─► validateTestFlow()
                              ├─► generateTestDataVariations()
                              ├─► detectFlakiness()
                              ├─► suggestRefactoring()
                              └─► suggestSelector()
```

### Model Context Protocol

```
Test → MCPHelper → MCP Client → StdioTransport → MCP Server
                       │
                       ├─► navigate()
                       ├─► screenshot()
                       ├─► getPerformanceMetrics()
                       ├─► getNetworkRequests()
                       └─► getAccessibilityTree()
```

---

## 8. Security Architecture

```
┌───────────────────────────────────────────┐
│        Environment Variables               │
│        (.env file - gitignored)           │
├───────────────────────────────────────────┤
│  ANTHROPIC_API_KEY = sk-ant-...           │
│  SUPABASE_API_KEY = eyJ...                │
│  FASTMAIL_API_KEY = fmu1-...              │
└───────────────────────────────────────────┘
                  │
                  ▼
        ┌─────────────────┐
        │  dotenv loader   │
        └─────────────────┘
                  │
                  ▼
        ┌─────────────────┐
        │  process.env     │
        └─────────────────┘
                  │
        ┌─────────┴─────────┐
        ▼                   ▼
┌──────────────┐    ┌──────────────┐
│ AI Utilities │    │  DB Queries  │
└──────────────┘    └──────────────┘
```

---

## 9. Error Handling Flow

```
Test Execution
      │
      ├─► Success → Continue
      │
      └─► Failure
            │
            ▼
      ┌─────────────┐
      │  afterEach  │
      └─────────────┘
            │
            ├─► Check ANTHROPIC_API_KEY?
            │       │
            │       ├─► No → Skip AI analysis
            │       │
            │       └─► Yes
            │             │
            │             ▼
            │     ┌───────────────────┐
            │     │  AI Analysis      │
            │     ├───────────────────┤
            │     │ 1. Capture state  │
            │     │ 2. Send to Claude │
            │     │ 3. Get insights   │
            │     │ 4. Save results   │
            │     │ 5. Log to console │
            │     └───────────────────┘
            │             │
            │             ▼
            │     ┌───────────────────┐
            │     │  Error caught?    │
            │     ├───────────────────┤
            │     │ • Log warning     │
            │     │ • Continue test   │
            │     │ • Don't fail test │
            │     └───────────────────┘
            │
            ▼
      ┌─────────────┐
      │   Cleanup   │
      └─────────────┘
```

---

## 10. Performance Optimization

```
┌────────────────────────────────────────────┐
│         Performance Strategy                │
├────────────────────────────────────────────┤
│                                             │
│  Conditional Execution:                     │
│  • AI only runs on failures                 │
│  • MCP only when explicitly used            │
│  • API calls are cached where possible      │
│                                             │
│  Lazy Loading:                              │
│  • Anthropic client created on first use    │
│  • MCP connection only when initialized     │
│                                             │
│  Resource Management:                       │
│  • Automatic cleanup in fixture teardown    │
│  • Connection pooling for API calls         │
│  • Screenshot compression                   │
│                                             │
│  Parallel Execution:                        │
│  • Tests run in parallel (Playwright)       │
│  • AI analysis doesn't block test progress  │
│  • Reporter summarizes at end               │
│                                             │
└────────────────────────────────────────────┘
```

---

## Summary

This architecture provides:

✅ **Modular Design** - Each component is independent
✅ **Scalable** - Easy to add more AI features
✅ **Performant** - Minimal overhead on test execution
✅ **Secure** - API keys managed via environment variables
✅ **Flexible** - AI features are optional and conditional
✅ **Observable** - Comprehensive reporting and logging

The framework seamlessly integrates AI capabilities while maintaining the simplicity and reliability of your existing Playwright tests.
